$('.book_comment-<%= @book.id %>').html("<%= escape_javascript(render partial: 'book_comments/book_comment', locals: {book: @book, book_comment: @book_comment}) %>");
$(document).ready(function () {$('#book_comment_comment').val('');});